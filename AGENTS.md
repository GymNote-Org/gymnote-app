# GymNote 프로젝트 가이드

이 문서는 저장소 전체에 적용되는 개발 가이드다. 프로젝트 구조나 데이터 계약이 바뀌면 관련 코드와 함께 갱신한다. `CLAUDE.md`는 이 파일을 가리키는 상대 심볼릭 링크이며, 공통 지침은 `AGENTS.md`에서 관리한다.

## 프로젝트 개요

GymNote는 날짜별 운동과 세트를 기록하는 모바일 중심 앱이다. 무료로 사용 가능한 기술과 배포 방식을 지향한다.

버전 1은 로그인 없는 PWA다. HTML, CSS, 브라우저 ES 모듈 JavaScript와 IndexedDB를 사용하며, 런타임 패키지 의존성이나 백엔드 서버, 유료 API가 없다. 별도 빌드나 의존성 설치 없이 정적 파일을 제공한다. 사용자 화면과 안내 문구는 한국어를 기본으로 한다.

문서의 프로젝트명은 `GymNote`로 표기한다. IndexedDB 이름, 백업 식별자, 운동 ID 등 데이터에 쓰이는 이름은 표시용 브랜드명과 구분하고 호환성을 유지한다.

## 전체 프로젝트 구조

```text
gymnote-app/
├── .github/
│   └── workflows/
│       └── pages.yml           # 수동 실행하는 GitHub Pages 배포
├── .gitignore                  # 환경변수, 캐시, 로컬 배포 설정 등 제외
├── AGENTS.md                   # 프로젝트 공통 개발 지침 원본
├── CLAUDE.md -> AGENTS.md       # 공통 지침을 참조하는 상대 심볼릭 링크
├── LICENSE                     # MIT 라이선스
├── README.md                   # 기능, 실행, 설치, 배포, 데이터 보관 안내
├── package.json                # ES 모듈 설정과 dev/test/check 명령
├── dist/                       # 직접 작성하는 앱 소스이자 배포 대상
│   ├── index.html              # 한국어 문서, 앱 진입점, PWA 메타데이터
│   ├── app.js                  # 화면 렌더링, 이벤트, 저장 흐름, 공유/복원
│   ├── model.js                # 운동 목록, 검증, 날짜, 볼륨, JSON/CSV
│   ├── storage.js              # IndexedDB 연결과 읽기/쓰기 트랜잭션
│   ├── webmcp.js               # 지원 브라우저의 선택적 도구 인터페이스
│   ├── style.css               # 공통 테마 토큰과 기본 컴포넌트 스타일
│   ├── responsive.css          # 조회/백업 화면 스타일과 반응형 규칙
│   ├── sw.js                   # 정적 파일 캐시와 오프라인 처리
│   ├── manifest.webmanifest    # 홈 화면 설치, 앱 이름, 색상, 아이콘 설정
│   ├── icon.svg                # 벡터 앱 아이콘
│   └── icons/
│       ├── icon-192.png        # 192 × 192 설치 아이콘
│       ├── icon-512.png        # 512 × 512 설치 아이콘
│       └── maskable-512.png    # 마스킹을 고려한 512 × 512 아이콘
├── scripts/
│   └── check.mjs               # 문법, 파일 참조, 아이콘, PWA 캐시 검사
└── tests/
    └── model.test.mjs          # Node 내장 테스트 러너로 모델 검증
```

로컬 환경에는 Git에서 제외된 `.openai/hosting.json`이 있을 수 있다. 이는 Sites 검토용 배포 설정이며 GitHub Pages 실행에 필요하지 않다. `dist/`는 생성 후 버리는 빌드 결과물이 아니므로 삭제하거나 `.gitignore`에 추가하지 않는다.

## 핵심 기능

- 날짜별 운동 선택, 세트 추가·삭제, 세트별 중량(kg)과 반복 횟수 입력
- 세트별 볼륨을 합산한 운동별·일일 총 볼륨과 주간 그래프
- IndexedDB 자동 저장, 저장 실패 표시와 재시도
- 월별 달력과 지난 운동 기록 조회
- 선택 기간의 JSON 백업 및 CSV 내보내기, JSON 복원
- OS 파일 공유로 Google Drive 등 사용자가 선택한 앱에 전달하고, 미지원 시 다운로드 제공
- 홈 화면 설치와 최초 온라인 캐시 완료 후 오프라인 사용

운동 목록의 기준은 `dist/model.js`의 `EXERCISES`다. 현재 5개 부위, 14개 운동을 제공한다.

| 부위 | 운동 |
| --- | --- |
| 가슴 | 체스트 프레스, 벤치 프레스, 체스트 플라이 |
| 어깨 | 밀리터리 프레스, 사이드 레터럴 레이즈, 리버스 팩덱 플라이 |
| 등 | 랫 풀 다운, 시티드 로우, 원 암 덤벨 로우 |
| 팔 | 트라이셉스 익스텐션, 케이블 푸쉬 다운, 덤벨 컬 |
| 하체 | 레그 프레스, 레그 익스텐션 |

## 핵심 패턴과 데이터 계약

### 모델과 화면의 역할 분리

계산, 날짜 처리, 검증, 내보내기는 DOM에 의존하지 않는 `model.js`에 둔다. IndexedDB 접근은 `storage.js`, 화면과 사용자 이벤트는 `app.js`에서 관리한다. 순수 로직을 화면 이벤트마다 중복 구현하지 않는다.

`app.js`는 `days`, `selectedDate`, `view` 등의 모듈 상태로 화면을 구성한다. `view`는 `log`, `history`, `backup`이며 별도 URL 라우터는 없다. `data-action`, `data-view`, `data-date`, `data-field` 등의 속성과 문서 수준 이벤트 위임을 사용한다.

입력 중에는 `updateTotals()`와 `updateSaveStatus()`로 필요한 부분만 갱신한다. 키 입력마다 전체 `render()`를 호출하여 포커스나 입력 위치가 사라지지 않도록 한다. 사용자 입력을 HTML 문자열에 넣을 때는 `escapeHtml`을 사용한다.

### 기록 형식과 계산

```js
{
  date: '2026-09-10',
  exercises: [
    {
      id: 'bench-press',
      sets: [
        { weight: '60', reps: '10' },
        { weight: '', reps: '' }
      ]
    }
  ],
  updatedAt: '2026-09-10T06:00:00.000Z'
}
```

- 날짜는 로컬 달력 기준 `YYYY-MM-DD`, 허용 범위는 `1900-01-01`부터 `2100-12-31`이다. `dateKey`, `parseDate`, `shiftDate`, `validDate`를 사용한다. 날짜 키를 UTC `toISOString()`으로 잘라 만들면 시차로 날짜가 달라질 수 있다.
- `weight`와 `reps`는 문자열로 보관한다. 빈 문자열은 작성 중인 값이므로 숫자 `0`으로 일괄 변환하지 않는다. `updatedAt`은 저장 시각 문자열이며 아직 저장하지 않은 기록은 `null`일 수 있다.
- 중량은 0-2,000 kg, 소수점 둘째 자리까지다. 반복 횟수는 1-999회 정수다. 운동당 세트는 1-100개이며 같은 날짜에 같은 운동 ID를 중복 추가하지 않는다.
- 세트 볼륨은 `중량 × 반복 횟수`, 운동과 일일 볼륨은 해당 세트들의 합이다. 세트별 값을 합산한 뒤 세트 수를 다시 곱하지 않는다.
- `completeSet`이 참인 세트만 기록한 세트 수와 볼륨 계산에 포함한다. 유효한 0 kg 세트는 세트 수에는 포함하고 볼륨은 0이다. 한쪽씩 하는 운동의 중량이나 반복을 임의로 두 배 하지 않는다.
- 소수 중량 계산은 기존 `setVolume`의 100배 정수 변환·반올림 방식을 유지한다. 포맷된 숫자 문자열을 다시 계산에 사용하지 않는다.

### 기기 내 저장과 실패 처리

IndexedDB 이름은 `gymnote`, 데이터베이스 버전은 `1`, 객체 저장소는 `days`, 키는 `date`다. `openDatabase()`와 `loadDays()`는 읽은 기록을 `validateDays()`로 검사한다. `saveDays()`는 전달된 날짜들을 하나의 쓰기 트랜잭션으로 저장하고 완료 이벤트에서 성공을 반환한다.

화면의 자동 저장은 `persistCurrent()`에서 스냅샷을 만들고 `writeQueue`로 직렬 처리한다. `pendingWrites`는 대기 중인 저장 수, `dirtyDates`는 저장 확인이 필요한 날짜를 관리한다. 저장 실패 시 입력 내용을 보존하고 오류와 재시도 경로를 제공한다. 실패를 무시한 채 저장 완료로 표시하거나 저장소를 초기화하지 않는다.

데이터베이스 이름·버전·구조를 변경할 때는 기존 기록의 마이그레이션을 고려한다. 저장은 브라우저 프로필과 오리진에 종속되며 동일 오리진의 경로만 달라진다고 별도 데이터베이스가 되는 것은 아니다. 기기 간 동기화나 다중 탭 동시 편집 충돌 해결 기능은 현재 구현되어 있지 않다.

### 백업과 복원

JSON 백업은 `app: 'gymnote'`, `version: 1`, `exportedAt`, `from`, `to`, `days` 필드를 사용한다. 데이터 식별자와 운동 ID를 표시명 변경 때문에 바꾸지 않는다. 형식을 바꾸면 기존 백업을 읽는 경로를 함께 유지한다.

내보내기 기간은 시작일과 종료일을 모두 포함하며, 운동이 없는 날짜는 제외한다. CSV는 UTF-8 BOM과 CRLF 줄바꿈을 사용하고 모든 필드를 큰따옴표로 감싼다. 세트당 한 행을 내보내며 빈 세트의 볼륨은 빈 필드로 표시한다. CSV는 조회용이며 복원 입력은 JSON만 지원한다.

복원은 파일 크기 5 MB 제한과 `readBackup()`/`validateDays()` 검증을 거친다. 알 수 없는 운동, 중복 날짜·운동, 허용 범위를 벗어난 값은 거부한다. 사용자가 복원을 확정한 뒤 저장하며, 겹치는 날짜는 덮어쓰고 다른 날짜는 유지한다. 기존 기록을 비우고 파일을 읽는 순서로 구현하지 않는다.

파일 공유는 `navigator.canShare({ files })`로 지원 여부를 확인한다. 공유 창 종료를 클라우드 업로드 성공으로 간주하지 않는다. 미지원 시 파일 다운로드를 제공하며, 클라우드 자동 동기화로 안내하지 않는다.

### PWA와 선택적 WebMCP

정적 파일 경로와 manifest의 `start_url`, `scope`는 상대 경로를 유지하여 `/gymnote-app/` 같은 하위 경로에서도 실행되게 한다. 외부 CDN이나 원격 폰트 등 오프라인 실행에 필요한 새로운 네트워크 의존성을 신중하게 다룬다.

배포되는 앱 파일을 바꾸면 `sw.js`의 `CACHE` 이름을 갱신한다. 파일을 추가하거나 경로를 변경하면 `ASSETS`도 맞춘다. 현재 탐색 요청은 네트워크 우선·오프라인 시 캐시 대체, 등록된 정적 파일은 캐시 우선이다. 새 서비스 워커는 기존 앱 창이 닫힌 뒤 활성화될 수 있다. 캐시 갱신을 위해 IndexedDB 기록을 삭제하지 않는다.

WebMCP는 `document.modelContext?.registerTool`을 감지한 뒤 등록하는 선택 기능이다. 일반 UI와 같은 검증·저장 경로를 사용하고, 미지원 환경에서도 기록 기능이 동작해야 한다. 조회, 날짜 이동, 운동 추가 도구의 실제 부작용과 설명·스키마를 일치시킨다.

## 개발 유의사항

- 작업 전 `git status`를 확인하고 사용자의 기존 변경을 보존한다. 요청 범위 밖의 대규모 정리나 프레임워크 전환을 섞지 않는다.
- 버전 1의 기기 내 저장·로그인 없음·무료 기술 원칙을 유지한다. 차기 버전의 요구가 생기면 해당 버전의 변경임을 코드와 문서에 구분한다.
- 공통 색상은 `style.css`의 `:root` 토큰으로 관리한다. 현재 방향은 어두운 차콜 배경, 파란 주요 버튼·선택 상태, 녹색 기운이 강한 라임 볼륨·운동 표시다. 반응형 스타일은 뒤에 로드되는 `responsive.css`의 재정의도 확인한다.
- 테마나 아이콘 변경 시 HTML의 `theme-color`, manifest 색상, SVG와 설치용 PNG 사이의 일관성을 확인한다. 텍스트 대비, 키보드 포커스, 입력 라벨과 모바일 터치 영역을 유지한다.
- 사용자에게 표시하는 오류는 한국어로 작성한다. 입력 범위 변경 시 UI 속성·오류 문구·모델 검증·백업 검증·WebMCP 스키마·테스트를 함께 확인한다.
- 문서의 숫자 범위는 `0-2,000`처럼 하이픈으로 표기하여 Markdown 취소선 해석을 피한다.
- 비밀 값, `.env`, 로그, 캐시, 로컬 `.openai/` 설정은 Git에 넣지 않는다. 배포용 `dist/`, 테스트, 문서와 `.github/workflows/pages.yml`은 추적한다.
- `CLAUDE.md`를 별도 사본으로 대체하지 않는다. 저장소 이동 후에도 연결되도록 링크 대상은 상대 경로 `AGENTS.md`를 유지한다.

## 실행과 검증

Node.js 22 이상과 Python 3을 사용하며, 현재 패키지 설치 단계는 없다. 저장소 루트에서 실행한다.

```sh
npm run dev       # http://127.0.0.1:4173 에서 dist/ 제공
npm test          # 계산·날짜·JSON·CSV·유효성 모델 테스트
npm run check     # JS 문법·HTML 참조·PNG 크기·PWA 캐시 파일 검사
git diff --check  # 변경 내용의 공백 오류 확인
```

로직이나 배포 파일을 수정하면 해당 검증을 실행하고, 변경한 동작에 필요한 테스트를 보완한다. 문서만 수정할 때는 내용·링크·심볼릭 링크와 diff를 확인하면 된다. 불필요한 의존성이나 구현을 그대로 복제하는 테스트를 추가하지 않는다.

자동 검증은 실제 브라우저의 IndexedDB, 서비스 워커 동작, 설치, 파일 공유나 시각적 검증을 대신하지 않는다. 해당 흐름을 변경하여 기기·브라우저 검증을 수행할 때는 수행 범위와 미검증 항목을 구분해서 보고한다.

## 배포

`dist/` 전체가 HTTPS 정적 호스팅 대상이다. `.github/workflows/pages.yml`은 `workflow_dispatch`로 수동 실행하며, Node.js 22에서 테스트와 파일 검사를 통과한 뒤 `dist/`를 GitHub Pages에 배포한다. 일반적인 Git 푸시만으로 이 워크플로가 실행되지는 않는다.

Sites 검토용 URL의 플랫폼 접근 제어와 앱의 로그인 기능은 별개다. 검토용 주소에 로그인이 필요하더라도 앱에 인증 로직이 추가된 것으로 설명하지 않는다. 배포한 주소나 오리진을 바꿀 때 기존 기기 기록의 보관 위치와 JSON 이전 필요성을 고려한다.
